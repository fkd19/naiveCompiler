#pragma once
#include "define.h"

using namespace std;

class one_mid_code
{
public:
    one_mid_code(void);
    ~one_mid_code(void);
    char op[ALNG];
    char rd[ALNG];
    char rs[ALNG];
    char rt[ALNG];
};

